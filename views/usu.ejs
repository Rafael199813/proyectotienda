    <%- include ./header.ejs %>

    <div class="container">
        <div class="row">
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/add2" method="POST">

                            <div class="form-group">
                                <input type="text" name="nombre" placeholder="Inserte nombre" class="form-control" required >
                            </div>
                            <div class="form-group">
                                <input type="text" name="correo" placeholder="Inserte correo" class="form-control" required >
                            </div>
                            <div class="form-group"><input type="text" name="password" placeholder="Inserte contraseña" class="form-control" required>
                            </div>
                            <div class="form-group">
                                <input type="text" name="usuario" placeholder="Inserte usuario" class="form-control" min="1" title="El precio debe ser mayor a 1" required >
                            </div>
                            <button type="submit" class="btn btn-primary btnblock">
                                Agregar
                            </button>
                        </form>
                    </div>
                </div>
            </div>
            <div class="col-md-7">
                <table class="table table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>NÂ°</th>
                            <th>Nombre</th>
                            <th>Correo</th>
                            <th>Password</th>
                            <th>Usuario</th>
                        </tr>
                    </thead>
                    <% for(var i=0; i< tasks.length; i++) { %>
                        <tr>
                            <td>
                                <%= i + 1 %>
                            </td>
                            <td>
                                <%= tasks[i].nombre %>
                            </td>
                            <td>
                                <%= tasks[i].correo %>
                            </td>
                            <td>
                                <%= tasks[i].password %>
                            </td>
                            <td>
                                <%= tasks[i].usuario %>
                            </td>
                            <td>
                                <a href="/delete/<%= tasks[i]._id%>" class="btn btn-danger">Borrar</a>
                                <a href="/edit/<%= tasks[i]._id%>" class="btn btn-info">Editar</a>
                            </td>
                        </tr>
                        <% } %>
                            <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
    <%- include ./footer.ejs %>
</body>
</html>